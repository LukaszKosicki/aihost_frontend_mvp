server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # (opcjonalnie) proxy do backendu ASP.NET Core
  # location /api/ {
  #   proxy_pass http://backend-service:5000/;
  # }
}
